version: "2"

linters:
  default: none
  enable:
    - loglint
  settings:
    custom:
      loglint:
        type: module
        description: Checks log messages in slog/zap
        settings:
          rules:
            lowercase: true
            english: true
            special-symbols: true
            sensitive-data: true
          sensitive-keywords:
            - password
            - token
          sensitive-patterns:
            - '(?i)ssn\\s*[:=]'
